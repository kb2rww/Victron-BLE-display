#include "ui.h"
#include "datasource.h"
#include "storage.h"

void setup() {
  Serial.begin(115200);

  uiInit();          // TFT, touch, backlight
  storageInit();     // NVS
  loadSettings();    // Load saved config

  datasourceInit();  // Init BLE or TTL based on config

  uiDrawCurrentPage();
}

void loop() {
  uiHandleTouch();
  datasourcePoll();
  uiUpdate();
}
``
